{
  "name": "rrpt-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express API + pluggable storage (JSON / MongoDB) for the Ryan Reynolds Physique Tracker",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js"
  },
  "engines": {
    "node": ">=20.18.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mongodb": "^7.5.0"
  }
}
